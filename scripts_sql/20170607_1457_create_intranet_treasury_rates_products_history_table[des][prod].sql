CREATE TABLE INTTRRAPRH (
    ID VARCHAR(45),
    DATE_ID VARCHAR(45),
    NAME VARCHAR(100),
    RATE_TYPE CHAR(1), --P = pasivo, A = activo
    CONTENT CHAR(1),--R = rangos, V = valores, U = unico
    RANGES VARCHAR(500)
)
